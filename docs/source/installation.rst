Installation
============

* Install Django Perseus::

    pip install django-perseus


* Add ``'django_perseus'`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # ...
        "django_perseus",
    )
